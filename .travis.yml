---

group: stable
dist: trusty
language: java

jdk:
  - oraclejdk8

branches:
  only:
    - master

# tmp fix for travis error
sudo: required

addons:
  chrome: stable

cache:
  directories:
  - $HOME/.m2

script:
  - make test.unit
  - make test.site
