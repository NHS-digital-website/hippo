---

group: stable
dist: trusty
language: java

jdk:
  - oraclejdk8

branches:
  only:
    - master

addons:
  chrome: stable

cache:
  directories:
  - $HOME/.m2

script:
  - make test.unit
  - make test.site
