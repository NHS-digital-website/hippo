#!/bin/bash

## Return OnDemand environment name

case $1 in
    dev)
        echo "dev";
        ;;
    prd)
        echo "prod";
        ;;
    trg)
        echo "training";
        ;;
    tst)
        echo "test";
        ;;
    uat)
        echo "acct";
        ;;
    *)
        echo "unknown"
        ;;
esac;
