package uk.nhs.digital.apispecs.swagger.request.bodyextractor;

import java.util.LinkedHashMap;

class RequestBodyMediaTypeObjects extends LinkedHashMap<String, RequestBodyMediaTypeObject> {
    // nothing here because we're only using it to give the (super) map a name
}
