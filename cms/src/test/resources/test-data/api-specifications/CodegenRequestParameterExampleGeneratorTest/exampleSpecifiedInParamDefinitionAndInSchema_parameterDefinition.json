{
  "example": "simple example specified in parameter's definition with HTML hostile characters < > &",
  "schema": {
    "example": "example specified in parameter's schema with HTML hostile characters < > &"
  }
}