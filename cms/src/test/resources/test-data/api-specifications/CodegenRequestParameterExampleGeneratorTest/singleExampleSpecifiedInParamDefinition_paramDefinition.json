{
  "examples": {
    "first": {
      "summary": "Example A from request parameter's definition - summary with HTML hostile characters < > &",
      "description": "Example A from request parameter's definition - description with `Markdown`",
      "value": "Example A from request parameter's definition - value with HTML hostile characters < > &"
    }
  }
}