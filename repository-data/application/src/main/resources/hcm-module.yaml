---
definitions:
  config:
    /hippo:configuration/hippo:queries/hippo:templates/new-image-folder/hippostd:templates/image gallery:
      hippostd:gallerytype:
        operation: override
        type: string
        value:
        - website:CorporateImageSetSpecification
group:
  after: hippo-cms
  name: publicationsystem
module:
  name: website-repository-data-application
project: publicationsystem
