package uk.nhs.digital.apispecs.swagger.model;

import java.util.LinkedHashMap;

public class MediaTypeObjects extends LinkedHashMap<String, MediaTypeObject> {
    // nothing here because we're only using it to give the (super) map a name
}
